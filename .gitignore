node_modules/
dist/
*.vsix
.vscode-test/
coverage/
test-results/
playwright-report/
.env
.env.*

# Script locali (non committare)
install-ozbridge.ps1
test-ozbridge.ps1

# Local agent / tooling state (never published)
.claude/
.remember/
ruvector.db
*.ruvector.db

# Local audit / planning working docs (kept out of the published tree)
/*[Aa]udit*.md

# Internal go-to-market playbooks. These reference the private Warp trademark
# permission and per-channel posting strategy — the permission stays private,
# so the documents that discuss it must never be published.
docs/launch/

# Internal working docs: competitive positioning (names rival extensions) and
# the per-release PR execution playbooks. Developer-only, not user-facing.
docs/*Roadmap competitiva*.md
docs/NEXT-STEPS-v*.md

# MCPB bundle staging + output (regenerated by scripts/build-mcpb.mjs)
dist-mcpb/

# Python (media + asset-compliance tooling under scripts/)
__pycache__/
*.py[cod]
.venv/
# Asset OCR-compliance audit sidecars (regenerated by verify_baked_text.py).
# Written next to the asset, so match at any depth (e.g. media/launch/).
**/*.compliance.json

# AI tooling state & config (personal, never published)
.claude-flow/
graphify-out/
.mcp.json
.graphifyignore
/CLAUDE.md

# Machine-specific local convenience script (hardcoded Windows path)
scripts/start-mcp-server.ps1

# Working intermediates for the launch OG-card (regenerated during asset build)
media/launch/_ogcard_*.png
# Unverified hi-res OG-card variants — must be regenerated and OCR-verified
# against scripts/asset-compliance.json (via scripts/verify_baked_text.py)
# before use. Keep unverified variants out of the tree.
media/launch/social-og-card-1280.*
