node_modules/
dist/
*.vsix
.vscode-test/
coverage/
test-results/
playwright-report/
.env
.env.*

# Script locali (non committare)
install-ozbridge.ps1
test-ozbridge.ps1

# Local agent / tooling state (never published)
.claude/
.remember/
ruvector.db
*.ruvector.db

# Local audit / planning working docs (kept out of the published tree)
/*[Aa]udit*.md

# Python (media + asset-compliance tooling under scripts/)
__pycache__/
*.py[cod]
.venv/
# Asset OCR-compliance audit sidecars (regenerated by verify_baked_text.py).
# Written next to the asset, so match at any depth (e.g. media/launch/).
**/*.compliance.json

# AI tooling state & config (personal, never published)
.claude-flow/
graphify-out/
.mcp.json
.graphifyignore
/CLAUDE.md

# Machine-specific local convenience script (hardcoded Windows path)
scripts/start-mcp-server.ps1

# Working intermediates for the launch OG-card (regenerated during asset build)
media/launch/_ogcard_*.png
# Unverified hi-res OG-card variants — the OG card is an open launch blocker
# (compliancePass:false per docs/launch/LAUNCH-KIT.md, must be regenerated and
# OCR-verified before use). Keep unverified variants out of the tree.
media/launch/social-og-card-1280.*
