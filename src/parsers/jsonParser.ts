// IMPL: re-export toolkit's JSON parser — identical 5-level strategy, uses CliError
export { parse, parseOrThrow, parseOrValidate } from 'copilot-chat-toolkit';
export type { ParseResult } from 'copilot-chat-toolkit';
